@model IEnumerable<Notice>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminE/Views/Shared/_AdminLayout.cshtml";
    int count = 0;

}
@if (Model.Count() > 0)
{


    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Course Table</h4>
                    <p class="card-description">
                        <a asp-controller="notice" asp-action="Create" class="btn btn-primary">Create</a>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Count</th>
                                        <th>Title</th>
                                        <th>Date</th>
                                        <th>Setting</th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @foreach (var Model in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@Model.Title</td>
                                            <td>@Model.Date</td>
                                            <td>
                                                <a class="btn btn-danger" asp-controller="notice" asp-action="Delete" asp-route-id="@Model.Id">Delete</a>
                                                <a class="btn btn-dark" asp-controller="notice" asp-action="Update" asp-route-id="@Model.Id">Update</a>
                                                <a class="btn btn-success" asp-controller="notice" asp-action="Detail" asp-route-id="@Model.Id">Detail</a>

                                            </td>
                                        </tr>
                                    }

                                </tbody>
                            </table>

                        </div>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <a asp-controller="notice" asp-action="Create" class="btn btn-primary">Create</a>
    <h1>Bosdu</h1>
}
