@model IEnumerable<Blog>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminE/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
    count++;
}
@if (Model.Count()>0)
{
    

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Blog Table</h4>
                <p class="card-description">
                    <a asp-controller="Blog" asp-action="Create" class="btn btn-primary">Create</a>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Count</th>
                                    <th>Image</th>
                                    <th>Title</th>
                                    <th>Author</th>
                                    <th>WriterTime</th>
                                    <th>Comments</th>
                                    <th>Setting</th>
                                </tr>
                            </thead>
                            <tbody>

                                    @foreach (var Model in Model)                                    
                                    {
                                        <tr>
                                    <td>@count</td>
                                    <td><img style="width:200px;height:50px;" src="~/img/blog/@Model.Image" /></td>
                                    <td>@Model.Title</td>
                                    <td>@Model.Author</td>
                                    <td>@Model.WriteTime</td>
                                    <td>@Model.CommentCount</td>
                                    <td>
                                        <a class="btn btn-danger" asp-controller="Blog" asp-action="Delete" asp-route-id="@Model.Id">Delete</a>
                                        <a class="btn btn-dark" asp-controller="Blog" asp-action="Update" asp-route-id="@Model.Id">Update</a>
                                        <a class="btn btn-success" asp-controller="Blog" asp-action="Detail" asp-route-id="@Model.Id">Detail</a>

                                    </td>
                                </tr>
                                    }

                            </tbody>
                        </table>

                    </div>
            </div>
        </div>
    </div>
</div>
}
else{
     <a asp-controller="Blog" asp-action="Create" class="btn btn-primary">Create</a>
    <h1>Bosdu</h1>
}

